<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jbk.rxjavalearn">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".rxjavaNetExample.view.RetrofitRxjavaActivity" />
        <activity android:name=".operator.create_operator.CreateOperatorActivity" />
        <activity android:name=".operator.change_operator.ChangeOperatorActivity" />
        <activity
            android:name=".operator.group_and_merge_operator.GroupAndMergeActivity"
            android:label="@string/title_activity_group_and_merge" />
        <activity
            android:name=".operator.function_operator.FunctionOperatorActivity"
            android:label="@string/title_activity_function_operator"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>